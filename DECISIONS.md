# Develop-HTA — Decision Log

One decision per row. A decision isn't made until it's logged: date · decision · rationale · data source. Locked decisions aren't relitigated mid-build — change the log first, with Max.

Project: website for **Develop-HTA**, a non-profit making Health Technology Assessment accessible across Sub-Saharan Africa. Founders Marina (MD) + Jani (biomedical engineer).
Build repo: `D:\AntiGravity\develop-hta` · Client source materials: `client-materials/` · Research: `research/`

## Locked decisions

| Date | Decision | Rationale | Source |
|------|----------|-----------|--------|
| 2026-08-11 | Build via Master Brain **Reference-Clone Elite Site** system, started with **Project Bootstrap** method (research → benchmark → owner-gated direction → build). No freestyle. | House rule: base every build on a proven form; premium marketing/NGO site routes here. | `~/.claude/CLAUDE.md` §1/§4; `D:\Master Brain\Systems\Reference-Clone Elite Site.md` |
| 2026-08-11 | Replace the existing amateur all-blue Wix-style draft; do not copy it. | Draft is off-bar (flat royal blue, generic AI globe, weak IA). | Client images 3-5 in `client-materials/` |
| 2026-08-11 | No fabricated proof. Real photos / expert bios / partner logos / registration = honest "content-needed" slots until the client supplies them. | Fabricated proof scores below honest slots with every fair judge. | `Reference-Clone Elite Site.md` "the league" |

| 2026-08-11 | **Reference**: clone mechanics from **iDSI (idsihealth.org)** + Our World in Data (knowledge-hub geometry); GiveWell/CGD for citation-transparency; PATH for SVG-motif-as-photo-substitute. | Benchmark Selection: iDSI is our literal in-class sibling (HTA for LMICs). | `research/reference-candidates.md`, `reference-teardown.md` |
| 2026-08-11 | **Design direction = "Evidence Institute"** (Max pick) — sober gridded scroll on the iDSI spine, grafting D3 citation density + one D2 bridge line-motif. | Highest credibility for ministries/donors, lowest fabrication, cheapest with no photos. | Owner-gate 2026-08-11; `research/DIRECTION-MEMO.md` |
| 2026-08-11 | **Scope = Phase 1 only**: Home, What we do, About us, Knowledge Hub, Contact. | Ships a complete credible site fast; Phase 2 waits on entity registration + real content. | Owner-gate 2026-08-11 |
| 2026-08-11 | **Typography = Source Serif 4 (headings) + Source Sans 3 (body/UI)**, self-hosted woff2. | Institute register, signals rigor without luxury-fashion; EU self-host avoids Google Fonts data concerns. | Owner-gate 2026-08-11 |
| 2026-08-11 | **Palette discipline** = navy ink ramp on clinical white + ONE functional whisper `--accent teal-600 #14808C` (AA-safe); green = mark + rare positive-data only; gradient only in logo + at most one hairline. | Raw teal #1A9AA8 / green #5CB85C fail WCAG AA for text; fixes the amateur all-blue draft. | Owner-gate 2026-08-11; `research/brand-and-content.md` §1.1 |

| 2026-08-11 | **Deployed to Vercel** (Max pick) — live at **https://develop-hta.vercel.app** (gm-ax team, project "site"). SSO/deployment protection disabled so the public marketing site is reachable. | Founders need a live link to review; static, free, zero-config. | Owner-gate 2026-08-11 |
| 2026-08-11 | Passed independent reference-judge (verdict FIX-FIRST → all 4 gaps fixed): privacy/legal footer + `privacy.html`, eyebrows thinned to 1/page, hero motif strengthened, values grid rebalanced to 6. | Builder-not-judge done-gate vs iDSI bar. | judge output 2026-08-11 |

| 2026-08-11 | **REBUILD v2 — cloned a real elite reference.** Max rejected v1 as generic AI slop (boring sections, hero unrelated to logo, kicker spam, no footer logo). Ran a reference-hunt workflow; Max picked **Wellcome Leap** (wellcomeleap.org) to clone. New language: **bold Archivo grotesque display, dark full-bleed generative hero, program-tile grid of on-brand canvas art (network/circuit/molecular/rings/burst), network "bridge" section.** Hero tied to the logo (circuit/network/medical-cross constellation). Real logo used: transparent PNG in header, white knockout in footer. Kicker subheadings removed. Generative visuals in `dhta-viz.js` (canvas), no stock photos. Teardown in `research/teardown-wellcomeleap.md`. | v1 was benchmark-SELECTION without cloning craft = generic. This is the Reference-Clone system done properly. | Max feedback 2026-08-11; `research/REFERENCE-SHORTLIST.md` |

| 2026-08-12 | **REBUILD v3 — Top-1% with real media + interactivity.** Max: still freestyled, no photos/video, no interactivity; directed to use the Master Brain Top-1% Standard + `impeccable` auditor + systems. Ran ultracode audit+media+plan workflow. Added: real color-graded stock VIDEO hero (Mixkit circuit board, graded navy/teal, seamless loop, 252KB, poster, reduced-motion paused) tied to the logo's circuit identity; REAL clinician photos in the 4 program tiles + bridge band + what-we-do zig (Pexels, dignified African/diverse healthcare, non-cliche, free-commercial); affiliations strip (WHO/PATH/HTAi, honest); interactive "How an assessment works" stepper (accessible accordion); Lenis smooth scroll + staggered reveals + 6-state buttons; funnel fix (hero primary CTA = "Get in touch"). Ran `impeccable` (22→16 findings; fixed side-tab/contrast/all-caps/type-sprawl; remaining are false-positive cramped-padding from the full-bleed+wrap pattern + 1 intentional accordion). Founder headshots = honest slot (no stock face as founder). | Top-1% Law 4 = real photography dominates; generative-only read as illustration/slop. | Max feedback 2026-08-12; `research/top1-audit.md`, `TOP1-BUILD-PLAN.md`, `media-shotlist.md` |

| 2026-08-12 | **Judge-fix round** (fresh-context judge vs Wellcome Leap + Top-1% = FIX-FIRST → all 5 fixed). Swapped hero from generic circuit to an on-message **vital-signs monitor** clip (teal ECG on navy = brand-matched, health-tech, no white-savior); punchy headline "Making health technology work for Africa." (no hyphenation, both CTAs above fold); pulled teal off the thesis headline; removed decorative tile category chips; lightened tile photo overlays; turned the plain CTA band into a warm photo closing band (real clinicians). `impeccable` 22→14 (remaining = false-positive full-bleed cramped-padding + display leading + small-label sizing). | Niche-footage rule: hero must be the customer's world, not generically beautiful. | judge output 2026-08-12 |

| 2026-08-12 | **System-upgrade pass** (Master Brain system auditor = FIX-FIRST: named Reference-Clone but never built its primitives). Ported the proven Gmax/Reference-Clone motion system 1:1: **Lenis(duration:0.9,expo)+GSAP-ticker** scroll (removed the floaty lerp + `scroll-behavior:smooth` conflict), **`.pill-liquid`** cuberto CTA (teal + `-onink` variants) on every funnel action, **fixed slide-down nav** (kept the underline-wipe), **masked-line heading reveal** (`.reveal-h`, yPercent:120->0, expo.out, hero on load / sections on ScrollTrigger). **Font -> Clash Display (display) + General Sans (body)**, self-hosted; deleted 48 dead woff2 (54->6). **Full-line hero headline** (removed the 13ch choke, widened column, 2 full lines). Wrote `research/technique-motion.md` (craft-asset gate). Extended `discipline-check.py` with drift rules (scroll-behavior:smooth / Lenis-lerp / gsap-missing = FAIL). impeccable 14 (all false-positive full-bleed). | Directives 1-6 + every audit finding. OPEN: scroll feel must be judged on Max's live trackpad (headless is blind). | Master Brain system audit + `UPGRADE-PLAN.md` 2026-08-12 |

| 2026-08-13 | **REDIRECT v5→v8 — cloned Function Health (Max's own pick), warm/photo-led.** v5 (Poppins+Mulish) + v6 (generative-canvas grid, photos removed) REJECTED. v7 niche-reference hunt (CGD/Gavi/iDSI) rejected as "not well designed." Max named his references: **Function Health = favorite** + charitywater.org + institute.global. Direction = warm cream `#FEF9EF`, elegant LIGHT SERIF **Fraunces** + Mulish + Fragment Mono, real dignified African-clinician photos, few DEEP sections, engaging (pinned scroll stepper, FAQ accordion, marquee, parallax), ONE shared `.wrap` grid, clear navy CTA pills. Built in `site/v8.html`+`css/v8.css`+`js/v8.js`+`parity-check-v8.py`; index.html untouched. FH teardown tokens in `research/v8/teardown-functionhealth.md`. | Max chose the reference himself after rejecting agent-picked niche institutes; owner-gate by SHOWING real sites, not abstract Q&A. | Max feedback 2026-08-13; `research/v8/` |
| 2026-08-13 | **ROOT-CAUSE: was BUILDING BLIND → fixed.** wmux screenshots time out, so every craft miss (mixed serif/sans, whitespace voids, kicker labels, weak photos, 2-grid misalignment) passed the mechanical gates GREEN. FIX = puppeteer-core screenshot loop I can Read (`scratchpad/shot.js`), now the standard done-gate: screenshot→Read→judge vs reference. Mechanical parity tests are necessary but NEVER sufficient. | Max: "can we fix you building blind?" The false-green pattern behind every rejected round. | memory `reference_visual_verification_pipeline.md` |
| 2026-08-13 | **v8 judged ELITE** by an independent fresh-context VISUAL judge (ran the screenshot loop): "a genuine top-1% clone of the Function Health craft, not an imitation," zero blockers. All 8 flagged issues fixed + verified by eye. Ship-quality pending Max's live-trackpad check of the pinned-stepper first frame + real founder photos. | Builder≠judge with SIGHT (not mechanical gates). Review gallery: claude.ai/code/artifact/4a9aed69-b30c. | judge output 2026-08-13 |

| 2026-08-13 | **v8 REJECTED by Max's eye — ~16 craft failures despite "ELITE" gate.** Ran an 8-agent ultracode postmortem (`research/v8/POSTMORTEM.md`). ROOT CAUSE (verify-corrected): Max's hard wants (video hero, centered, one font) were never captured as a spec, so the reference was never owner-gated against them → wrong reference (Function Health is left-aligned/3-font/photo-hero, the opposite of the ask) got locked → gates rewarded FH-fidelity, the wrong target. The token layer WAS data-driven; the Master Brain **Anti-Slop Deterministic Gate** + `impeccable` were never wired, the discipline linter pointed at a dead file (`dhta.css` not `v8.css`), and a green hygiene gate was mislabeled "ELITE." | Both adversarial verify passes returned REVISE and re-ranked the cause upstream + flagged an AI "would-Max-like-it" judge as a fake fix. | `research/v8/POSTMORTEM.md`; workflow wf_c1a5b27d-71b |
| 2026-08-13 | **FIX — spec-first, un-gameable gate.** (1) `owner-constraints.json` written BEFORE build-one = the locked spec; reference is checked against IT. (2) Mechanical hooks to add: rendered heading line-count ≤2 (390/768/1440), rendered-centering (measure bounding box, not the `text-align` property), font-family-count, no width-cap that stacks headings. (3) Run `impeccable detect` but WAIVE `cream-palette`+`single-font`+`overused-font:Fraunces` (owner-locked deliberate choices — verify caught that raw impeccable flags Max's own direction as slop); keep all real-defect rules. (4) A green gate = "ready to SHOW Max," NEVER "done/ELITE"; the gate never prints a quality verdict. Taste (photo register, "feels freestyled") stays with Max's eye by design, not routed into an AI judge. | Postmortem fix, both verify passes. Enforce at build-time, taste stays with Max. | `owner-constraints.json`; `D:\Master Brain\Systems\Anti-Slop Deterministic Gate.md` |
| 2026-08-13 | **Max's rebuild decisions (AskUserQuestion):** (a) **Reference** = keep Function Health palette/restraint but OVERRIDE 3 traits → centered, ONE font, VIDEO hero (Max accepts this is FH-flavoured, not a faithful clone). (b) **Approach** = ground-up rebuild (loop-risk flagged + accepted; mitigated by building hero+opening FIRST for Max's eye before the full page). (c) **Scope** = full stock build now, swap real client assets later. New build = `site/v9.*` (v8 left intact until v9 approved). Font pick = single-family Fraunces, waive the overused-font tell (deliberate brand choice). | Owner-gate; decisions are Max's, execution Claude's. | AskUserQuestion 2026-08-13; `owner-constraints.json` |

| 2026-08-13 | **RE-GROUNDED on the LIVE Function Health (I had been building off a token teardown, never the real site).** Findings: FH = cream + warm RUST/copper accent, serif display + sans body, LEFT hero but CENTERED section headings, italic-serif accent words, and a simple soft-rounded numbered CARD grid for "how it works" (a visual per card). v9 had drifted: navy/teal accent (I only borrowed FH's cream bg, never its warmth), ONE font, and an over-engineered pinned-scroll stepper. Max's own instincts (centered, visual-per-step, warm) actually MATCH FH; they clashed only with my bad execution. Max's re-lock: **accent = navy+teal (logo brand, accepts it's cooler than FH's rust); type = serif+sans (Fraunces + Mulish); layout = FH card pattern, drop the pinned stepper.** owner-constraints.json updated (font count 1->2 + notes). | Owner-gate after viewing the real FH side by side; the recurring failure was never actually studying the reference. | AskUserQuestion 2026-08-13; live functionhealth.com; `owner-constraints.json` |

| 2026-08-13 | **v10 = first build from the FH TEARDOWN BLUEPRINT (the right process at last).** Max's verdict on v9: ~0% vs the reference, because I cloned PAINT (color/font) not GEOMETRY+MOTION. Ran `site-teardown` on the real FH source → `research/functionhealth-teardown-blueprint.md` (every FH section = eye-hits + layout archetype + interaction). Built `site/v10.*` to those archetypes: real NAV w/ logo, **WebGL distortion-video HERO** (Three.js displacement + chromatic split over warm-graded abstract smoke `assets/video/hero-abstract.mp4`, Mixkit 12496; verified glOn=true), short decisive centered headline, pillars; **come-to-life cards** (SVG stroke draw-in on scroll); **evidence-body logo MARQUEE** (replaces "random words" chips; placeholder emblems, real logos TBD); **BENTO** grid; **comparison table** (with-HTA vs blind); founders; FAQ; **info-only footer (NO CTA** per Max). Lenis smooth scroll. Three.js vendored `js/vendor/three.module.js`. Verified via RECORDED-SCROLL gate (wheel-scroll capture): reveals fire 4/4 cards, 6/6 tiles, 5/5 rows, 2/2 founders. | The process fix embodied: teardown → archetype blueprint → build to geometry+motion → recorded-scroll gate (never stills/font-counts). | Max directive "apply master brain, use site-teardown, build now"; `research/functionhealth-teardown-blueprint.md` |

| 2026-08-13 | **v10 polish + MOBILE pass (7 Max changes, ultracode wf_354f8504 + wf_18f020c2).** Spec `research/v11-build-spec.md` + workflow output. Built, each routed to its MB skill (no freestyle): (1) FULL-SCREEN logo wall — static full-bleed grid, big marks, WHO+IQWiG real svgs + wordmarks; (2) VISIBLE hero distortion — shader center-floor 0.14->0.42, time .13->.22, amps up (edge-weighted still protects safe-framed subjects); (3) FLIP CARDS — "what we assess" tiles are buttons -> centered modal that grows from the tile + rotateY flip to "how we assess this" copy (emil+apple: focus-trap, inert, Esc, reduced-motion crossfade, mobile 94vw); (4) richer WORK-WITH-US = `.engage` (navy manifesto panel: why-it-matters + founder voice slot; Adopt/Negotiate/Decline verdict triad; copywriting skill); (5) removed hero pause button; (6) MOBILE pass — WebGL shader OFF <768px (plain video, perf), full-screen hamburger menu (fixed the backdrop-filter containing-block trap on .nav__in), comparison table 52px cols + full-bleed, pillars wrap, tap targets, logo-wall 2-col; (7) captured here. VERIFIED: 0 h-overflow @390, 0 console errors, flip opens desktop+mobile, all reveals fire mobile, no 404s. | Both adversarial verifies SOUND (parity + MB); fixes folded (logo wall made truly full-bleed, hamburger CSS ordering, mobile video-reveal). | Max's 7 asks; `research/v11-build-spec.md`; workflow specs |
| 2026-08-13 | **PRE-GO-PUBLIC flags (founders confirm via the Vercel preview):** WHO/IQWiG are REAL trademarked logos shown for the mockup — legal-clear or swap to wordmarks before public launch; founder quote (Marina & Jani) + the 6 flip "we assess" copies are honest placeholders to confirm; `contact@develop-hta.com` inbox must be live; `.em` italic is still faux (no `fraunces-italic.woff2`) — kicker removal (7 all-caps labels, a LOCKED-constraint pre-existing issue) also deferred/flagged, not done this round. | Site heads to a founder Vercel preview; these are the "they provide the rest of the missing info" items. | ultracode PART A |

| 2026-08-14 | **MB-audit + de-blog polish (ultracode wf_3a794269, 8 agents: master-brain-auditor + FH-width + cool-scroll + work-with-us + CTA/FAQ → synth → 2 adversarial verifies).** Both verifiers returned REVISE; I built the CORRECTED spec, not verbatim. Changes, each routed to its MB skill (no freestyle): (1) **de-blog width/flow** — `--maxw` 1180→1280 (+`--maxw-wide` 1360 on the `how`+`bento` bands), gutter tightened, section rhythm made asymmetric (top>bottom) + same-fill adjacent sections butt into one seam (`.how+.bento-sec`, `.bento-sec+.cmp`, `.engage+.faq` padding-top:0), inner caps widened (cmp 840→1040, founders 720→920, faq 760→860, pillars 940→1080). (2) **the "scroll line"** — 4-across `how` cards → a **draw-on-scroll vertical timeline**: scrubbed teal spine-fill + numbered nodes that light + alternating L/R cards settle in (scroll-motion-effects P3, natural budget, NO pin — degrades to a left-rail on mobile, full-visible on reduced-motion). Real geometry change, verified in screenshots. (3) **CTA fill** — base `.btn` is now a 3-layer liquid pill: label fixed above two pseudo-layers, accent **fills up** (scaleY bottom-origin) on hover/focus/active, tactile press-scale, reduced-motion = opacity fade (Reference-Clone §2 + emil/apple); dropped the opt-in `.btn--fill` token. (4) **+2 honest FAQ** (how paid / what evidence — no invented timeline). (5) work-with-us 2-col compact redesign was applied+screenshot-verified by the audit agent (keeps the interactive Adopt/Negotiate/Decline tabs + founder quote; fits ~1 viewport). Deleted dead pinned-stepper CSS. Fixed the pre-existing em-dash in `<title>`. **Corrected from the verifiers:** dropped the self-contradicting "no 3 consecutive same-bg" mechanical gate (it would false-fail the intentional cream run); **rewrote `discipline-check.py` scoped to v10 only** (naive repoint would break on 7 legacy dhta.css pages) — now PASSES; width probe kept as a screenshot judgment not a static-linter assert. Done-gate: linter green + screenshot→Read (desktop+mobile, all sections) + **fresh-context builder≠judge** vs the FH blueprint. | Max: "master-brain audit on how we've worked… too much like a blog, take more space, less gap, cooler scroll line for the 4-step, work-with-us too long, CTA fills on click." Routed through MB, verified builder≠judge. | Max feedback 2026-08-14; workflow wf_3a794269-666; `scratchpad/v11-*.png` |
| 2026-08-14 | **DEFERRED (flagged, not silently done): kicker-label removal.** The audit re-flagged the 8 all-caps kicker labels (a LOCKED house-rule). I did NOT bulk-delete them this round because Max explicitly praised the "rules we follow" section AS-IS (kicker included) and calls layout/copy an OWNER decision — removing a label from a section he called "perfect" risks a 2-strike. Needs Max's yes/no before removal. | Locked rule vs fresh specific praise = owner-gate the conflict, don't self-resolve. Decisions are Max's. | memory `feedback_no_kicker_labels`; Max 2026-08-13 praise |

| 2026-08-14 | **HOW-IT-WORKS reworked → horizontal pinned gallery (Max: timeline "too long vertically, not much info; rather it scroll and stay on the same vertical stance, cards more right to left").** Loaded `scroll-motion-effects` (GATE-0, no freestyle) → **P6 horizontal pin** (cookbook recipe): the `.how` section pins on ONE screen while `.how__track` translates `x: -(scrollWidth - clientWidth)`, `pin:true scrub:.5 start:'top top' end:'+=dist'` — cards scroll right→left, no wheel-capture, no dead scroll. JS adds `.how--horizontal` only above 820px + motion-on; **mobile / reduced-motion / no-JS fall back to a simple vertical card stack** (desktop≠phone). Cards enriched to 2-3 sentences each (Max: "doesn't provide much info") — honest, no invented numbers/timeline. Replaced the vertical draw-on-scroll timeline (which the judge flagged as too tall). VERIFIED by recorded wheel-scroll @1440: pin engages at scrollY≈2006 (pinTop→0), holds while trackX goes 0→-1072 (cards move R→L), releases exactly when cards finish (no dead scroll), flows into bento. Gate green; only 404 = favicon (benign). | Max directive; `scroll-motion-effects` P6 cookbook; `scratchpad/hscroll.js` trace + frames. | Max 2026-08-14; recorded-scroll verification |

| 2026-08-14 | **HOW-IT-WORKS → cloned FH's actual section (supersedes the horizontal pin).** Max: "it's not a scroll effect, use our reference site as inspo, no freestyling." Went back to the FH source and read the real "How it works" markup: `threecol_card` = numbered cards in a row, each with a **big accent number** (`threecol_card-number u-orange-highlight`), a visual that **comes to life on scroll** (Lottie), title + copy. No pin, no scroll-jack. Cloned it: 4 numbered cards (big teal serif numeral 01-04), rounded real photo, title, 2 substantive sentences, airy/borderless. Motion = FH's come-to-life reveal via P3 reveal-kit: number + card rise in + photo settles from a slow zoom, staggered L→R on scroll. Removed the P6 horizontal pin (Max: not what the reference does). One-screen tall. Gate green; layout screenshot-verified @1440. | The recurring lesson enforced: clone the reference's geometry+motion, don't invent an effect. FH's own "how it works" is come-to-life numbered cards, not a scroll-jack. | Max 2026-08-14; FH source `threecol_card`; blueprint row #3 |

## Open decisions (still owner-gate — awaiting Max / founders)

- [ ] **Kicker labels** — remove the 8 all-caps overlines (house-rule) or keep? Max praised the "rules we follow" section with its kicker, so this is his call before I strip them site-wide.

- [ ] **Custom domain** — point develop-hta.com DNS to the Vercel project when the founders are ready (currently on the .vercel.app URL).
- [ ] **Founder-confirm** — exact hybrid-model/division wording (legal, pre-registration), final hero copy.
- [ ] **Client assets** — real founder headshots + bios, partner/funder logos, EU base + registration status, privacy policy. Held as honest "coming soon" slots until supplied.
| 2026-08-14 | **HOW cards: photos -> line-art illustrations that draw themselves (matches FH's Lottie).** Max: "do the real illustration." Replaced the 4 step photos with a coherent inline-SVG line-art SET (speech bubble+dots = Question, magnifier-over-data = Evidence, balance scale = Appraisal, check-in-circle = Recommendation) on soft cream-2 panels, teal stroke, uniform weight/style. Come-to-life = stroke-draw on scroll via `pathLength=1` + GSAP `strokeDashoffset 1->0` (blueprint's sanctioned SVG line-draw = FH Lottie equivalent); no-JS/reduced-motion shows them fully drawn. VERIFIED live (reduce-off headless): dashoffset 1px at top -> 0px after scroll, zero errors. | Clone FH's actual mechanic (line-art that draws in), not photos; coherent icon SET per asset-set-coherence rule. | Max 2026-08-14; blueprint SVG line-draw mechanic |
| 2026-08-15 | **HOW one-line scroll: fixed the wide-monitor no-travel bug + duplicate photo.** Max: "one-line scroll isn't working, cards all on one line; cards 3 and 4 are the same picture." ROOT CAUSE of the dead scroll: `.hcard` width was `clamp(360px,42vw,560px)` — the 560px cap meant on a wide monitor 4 cards fit inside the viewport, so track.scrollWidth <= viewport, dist=0, pin had zero travel (headless 1440 hid it: dist was 992 there). FIX: `flex:0 0 max(360px,42vw)` (no upper cap) → 4 cards = 168vw, always overflows. Verified recorded-scroll: dist 1498 @1920, 1933 @2560 (was ~0). Duplicate: `step-04.jpg` == `card-advisory.jpg` (same desk shoot) — reassigned Appraisal(03)->`step-02.jpg` (distinct clinician-reviewing), Recommendation(04)->`card-advisory.jpg`, dropped step-04. All 4 photos now distinct. Gate green. | scroll-motion P6; verify on the REAL target width (wide monitor), not just 1440 — the cap bug was invisible at 1440. | Max 2026-08-15; hscroll trace @1920/2560 |
